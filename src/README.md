# Scheduler implementation

Place project-owned scheduling and optimization code here. Keep the supplied
evaluators, cases, configuration, and reference implementations in `official/`
unchanged; use `scripts/` to connect this code to the official evaluation flow.
